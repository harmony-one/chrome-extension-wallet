const H20 = require("./artifacts/h20.json");
const USDC = require("./artifacts/usdc.json");

export default {
  H20: {
    decimal: 18,
    artifacts: H20,
  },
  USDC: {
    decimal: 18,
    artifacts: USDC,
  },
};
